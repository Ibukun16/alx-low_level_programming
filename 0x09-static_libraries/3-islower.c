#include "main.h"

/**
 * _islower - A function that checks for lowercase character
 * @c: The variable contining lowercase alphabet
 *
 * Return: 1 if c is lower and 0 for otherwise.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
